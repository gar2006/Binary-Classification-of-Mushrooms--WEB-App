# Binary-Classification-of-Mushrooms--WEB-App
Streamlit web app that loads and encodes a mushroom dataset, lets users choose and tune a classifier (SVM, Logistic Regression, or Random Forest), trains the model, and evaluates it. It shows accuracy, precision, recall, and visual metrics to predict whether a mushroom is edible or poisonous.
